# Hunter HCC Hydrawise 灌溉控制器 Web 对接实施方案

## 1. 文档目的

本方案用于将 Hunter HCC（Hydrawise-ready）灌溉控制器接入校园智慧农场 Web 平台，实现灌溉状态查看、计划展示、运行记录留存，以及经授权后的远程人工启停。

本方案采用 Hunter Hydrawise 官方云平台和官方 API，不采用网页抓取、模拟登录或未授权的本地协议。

## 2. 已确认的设备能力

现场照片显示设备为 Hunter HCC 系列 Hydrawise-ready 控制器。该控制器可在本机离线执行已保存的灌溉计划；连接互联网后，可通过 Hydrawise 网页、手机应用及官方 API 进行远程管理。

Hydrawise 官方 REST API 可提供或操作的能力包括：

- 控制器名称和状态；
- 灌溉分区编号、名称及当前运行状态；
- 下次灌溉时间和预计运行时长；
- 手动启动、停止指定分区；
- 手动运行全部分区；
- 暂停指定分区或全部分区。

流量、雨量和土壤墒情等数据是否可用，取决于现场是否实际安装、接线并在 Hydrawise 中配置了相应传感器。

## 3. 推荐拓扑

```text
电磁阀 / 主阀 / 流量计 / 雨量或土壤传感器
                    │
                    │ 控制线或传感器线
                    ▼
          Hunter HCC 灌溉控制器
                    │
                    │ 2.4 GHz Wi-Fi（WPA/WPA2）
                    ▼
       校园物联网无线网络 / 路由器
                    │
                    │ 互联网 HTTPS
                    ▼
          Hunter Hydrawise 云平台
                    │
                    │ 官方 REST API 或 OAuth API
                    ▼
     校园智慧农场平台后端服务（服务器）
                    │
                    ▼
             校园智慧农场 Web 端
```

HCC 的标准云端联网方式为 Wi-Fi。设备可在断网时按本地计划继续灌溉，但断网期间无法通过 Web 平台获取最新状态或远程控制。现场设备说明要求无线网络为 2.4 GHz、802.11 b/g/n，并使用 WPA 或 WPA2；不建议使用需要网页认证的访客网络或仅支持 5 GHz 的网络。

## 4. 实施范围与边界

### 4.1 本期建议范围

- 查看控制器在线状态和最后同步时间；
- 查看分区、灌溉计划、当前运行分区和下次运行时间；
- 记录平台发起的操作和 Hydrawise 返回结果；
- 经授权后，手动启动、停止、暂停分区；
- 对设备离线、计划失败和传感器异常进行提示。

### 4.2 本期不纳入范围

- 通过未公开本地协议直接控制 HCC；
- 将 Hydrawise 账号密码、API Key 或 OAuth 密钥暴露给浏览器；
- 由平台自动修改全部灌溉计划；
- 未经现场确认即根据土壤数据自动开阀。

## 5. 前置条件及责任人

| 项目 | 完成标准 | 建议责任人 |
|---|---|---|
| 控制器识别 | 提供 HCC 完整型号、序列号、固件版本、安装位置 | 设备安装商 / 校方维护人员 |
| 阀门与分区清单 | 每个分区的编号、名称、种植区域、阀门类型和供水范围完整 | 农场管理老师 / 维护人员 |
| 网络 | 控制器位置具备稳定的 2.4 GHz WPA/WPA2 网络和互联网访问能力 | 校园网络管理员 |
| Hydrawise 账号 | 控制器已绑定学校持有的账号，明确主账号和交接人 | 校方项目负责人 |
| API 授权 | 已获得 REST API Key，或已向 Hunter 申请 OAuth 商业 API 权限 | Hydrawise 账号管理员 |
| 平台服务器 | 服务器可通过 HTTPS 访问 Hydrawise；具备密钥安全存储和任务调度能力 | 平台实施方 |
| 现场安全规则 | 明确各分区可灌溉时段、最大连续灌溉时间、人工停灌规则 | 农场管理老师 |

## 6. 可执行对接流程

### 步骤 1：完成网络准备

1. 校园网络管理员创建独立的 2.4 GHz 物联网 SSID，建议使用独立 VLAN 或独立无线网络。
2. 设置 WPA2-PSK 或兼容的 WPA/WPA2 密码认证；不要使用需要短信、账号登录或网页跳转认证的网络。
3. 在控制器安装位置测试无线信号和互联网连通性，记录 SSID、覆盖位置和网络责任人。
4. 如网络设备支持 DHCP 保留，为 HCC 分配固定地址，便于运维排查。该地址不作为平台直接控制接口使用。
5. 校园防火墙应允许 HCC 和平台服务器通过 HTTPS 访问 Hydrawise 服务；具体域名和放行策略以 Hunter 技术支持提供的清单为准。

### 步骤 2：完成 HCC 与 Hydrawise 绑定

1. 在 HCC 屏幕中进入网络或 Wireless 设置，选择上述 2.4 GHz Wi-Fi 并完成连接。
2. 使用学校持有的 Hydrawise 主账号绑定控制器；不得仅绑定在安装商个人账号下。
3. 在 Hydrawise 网页端确认控制器在线，记录控制器名称和控制器标识。
4. 按现场实际情况创建或核对每个灌溉分区的名称，例如“温室 A 区”“露天菜地 1 区”。
5. 为每个分区设置基础灌溉计划，并在现场进行一次人工启动和停止测试。
6. 如已安装流量计、雨量传感器或土壤传感器，逐一在 Hydrawise 中完成配置和读数核验。

### 步骤 3：取得 API 授权

#### 方式 A：REST API Key

适用于单校区、单账号或非商业性质的初期接入。由 Hydrawise 账号管理员执行：

1. 登录 Hydrawise 账号。
2. 进入 `Account Details`。
3. 在 `Account Settings` 中选择 `Generate API Key`。
4. 将 API Key 通过学校认可的安全方式交给平台实施方；不得通过普通群聊、邮件正文或前端页面传递。
5. 在平台服务器的密钥管理服务或环境变量中保存密钥，并限制读取权限。

#### 方式 B：OAuth API

适用于正式商用、多个控制器、需要更严格的账号授权或后续面向多学校推广的场景。由校方或实施方联系 Hunter/Hydrawise 申请 OAuth API 权限，获得应用标识、密钥、授权范围和测试环境信息后再接入。

校方智慧农场平台应优先选择 OAuth API。若当前仅可使用 REST API Key，应将其作为过渡方案，并在项目验收前确认其使用符合 Hunter 的 API 条款。

### 步骤 4：开发平台后端对接服务

1. 在后端建立 `hydrawise_client` 服务，统一负责鉴权、调用、重试、限流和审计。
2. 从 Hydrawise API 获取控制器清单，确认本项目控制器标识。
3. 同步控制器、分区、当前运行状态、下次运行时间和运行时长。
4. 将外部数据映射到平台数据模型，保留 Hydrawise 原始控制器 ID 和分区 ID，避免仅以名称关联。
5. 按官方 API 的频率限制进行轮询。建议常规状态每 5 分钟同步一次；运行中的分区可在 1 分钟级别刷新，但不得超过厂商限制。
6. 将每次请求时间、响应状态、错误码和数据更新时间写入运维日志。
7. 网络异常或 API 调用失败时采用指数退避重试；连续失败达到阈值后生成“设备或云服务不可达”告警。

建议的数据表至少包含：

| 数据表/实体 | 核心字段 |
|---|---|
| 控制器 | 外部控制器 ID、名称、在线状态、最后同步时间 |
| 灌溉分区 | 外部分区 ID、编号、名称、种植区域、状态 |
| 灌溉计划 | 分区 ID、计划类型、下次运行时间、预计时长 |
| 灌溉运行记录 | 开始时间、结束时间、分区、来源、结果、异常信息 |
| 操作审计 | 操作人、操作时间、控制指令、审批/确认信息、接口返回结果 |

### 步骤 5：实现 Web 端功能

1. 建立“灌溉总览”页面，展示控制器在线状态、当前灌溉分区、下次计划和异常提示。
2. 建立“分区管理”页面，展示分区名称、所属种植区域、灌溉计划和最近执行记录。
3. 建立“灌溉记录”页面，按日期、分区和操作来源查询历史记录。
4. 远程控制按钮仅向具有授权的角色展示。
5. 对“启动灌溉”“停止灌溉”“暂停全部分区”设置二次确认；启动操作必须填写或选择预计运行时长。
6. 控制前先读取当前状态，避免与已有计划或正在进行的灌溉冲突。
7. 控制后立即查询状态并提示“已受理/执行中/执行失败”，不得将 HTTP 请求成功直接等同于阀门已开启。

### 步骤 6：安全与运维

- API Key、OAuth Client Secret 和访问令牌只保存在后端，不写入浏览器代码、Git 仓库或日志。
- 按角色划分权限：浏览人员只读；农场管理员可发起控制；系统管理员负责账号和密钥。
- 任何远程开阀、停阀和全局暂停操作均记录操作人、原因和结果。
- 为每个分区配置最大允许连续灌溉时长；超时后由平台告警并通知现场人员核查。
- 保留 HCC 本地计划作为断网兜底，平台控制功能不可替代现场人工断水或紧急停阀措施。
- API Key 泄露、人员变动或安装商交接时，应立即轮换密钥并复核 Hydrawise 账号权限。

## 7. 验收标准

| 验收项 | 通过标准 |
|---|---|
| 网络连接 | HCC 可稳定连接 2.4 GHz 网络，并在 Hydrawise 显示在线 |
| 数据读取 | 平台可获取正确的控制器、分区、当前状态和下次灌溉信息 |
| 数据一致性 | 平台显示的分区状态与 Hydrawise 页面、HCC 现场状态一致 |
| 手动启动 | 经授权操作后，指定分区在设定时长内启动，平台记录完整 |
| 手动停止 | 停止指令后现场阀门停止，平台状态及日志正确更新 |
| 断网兜底 | 断开互联网后，HCC 仍按已保存的本地计划运行；平台给出离线提示 |
| 权限控制 | 未授权用户无法发起控制；所有控制操作可追溯 |
| 异常处理 | API 或网络连续失败时，平台产生明确告警且不无限重试 |

## 8. 待确认项

1. HCC 的完整型号、分区数量、固件版本和序列号；
2. 现场是否已经接入流量计、雨量传感器、土壤湿度传感器；
3. Hydrawise 账号是否归学校所有，安装商是否仍保有管理权限；
4. 校园网络是否可提供独立的 2.4 GHz WPA2 物联网网络；
5. 项目是否需要远程控制，还是仅做状态展示和记录；
6. 各灌溉分区的最大灌溉时长、允许灌溉时段和紧急停灌负责人；
7. 项目在 Hunter API 条款下应使用 REST API Key 还是申请 OAuth API。

## 9. 官方资料

- [Hunter Hydrawise API Information](https://www.hunterirrigation.com/support/hydrawise-api-information)
- [Hydrawise REST API Ver 1.6](https://www.hunterirrigation.com/sites/default/files/2026-05/Hydrawise%20REST%20API%20Ver%201.6_0.pdf)
- [Hunter HCC 支持页面](https://www.hunterirrigation.com/support/hcc-running-two-zones-simultaneously)

> 注：Hydrawise 的 API 路径、认证参数、频率限制和可用字段应以开通账号后取得的最新版官方 API 文档为准。本文不在代码或文档中固化真实账号、密钥或设备序列号。
