farm:
  demo-seed-enabled: false
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/smart_farm?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC}
    username: ${DB_USERNAME:smart_farm}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
