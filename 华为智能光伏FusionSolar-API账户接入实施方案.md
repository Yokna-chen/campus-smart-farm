# 华为智能光伏 FusionSolar API 账户接入实施方案

## 1. 文档目的

本方案用于将校园华为智能光伏系统接入智慧农场 Web 平台，获取发电量、实时功率、设备状态和告警等数据。

现场已识别的逆变器为 `SUN2000-5KTL-L1`。本方案明确采用 **FusionSolar 云端 API 账户接入**，不采用逆变器本地 Modbus TCP 接入。

## 2. 推荐拓扑

```text
华为 SUN2000-5KTL-L1 逆变器
              │
              │ Wi-Fi / 通信棒 / 现场网络
              ▼
       华为 FusionSolar 云平台
              │
              │ OAuth Connect / OpenAPI（HTTPS）
              ▼
校园智慧农场平台后端服务（服务器）
              │
              ▼
       校园智慧农场 Web 管理端
```

平台后端通过 FusionSolar API 获取数据，浏览器只调用校园平台自身接口。FusionSolar 账号密码、应用密钥、访问令牌均不得发送到浏览器或写入前端代码。

## 3. 接入范围

### 3.1 本期建议接入的数据

| 数据类别 | 建议字段 |
|---|---|
| 电站基础信息 | 电站名称、装机容量、时区、状态 |
| 发电数据 | 当前功率、当日发电量、累计发电量、统计时间 |
| 逆变器信息 | 设备名称、型号、序列号脱敏展示、运行状态、最后通信时间 |
| 运行参数 | 交流输出功率、电网侧状态、直流输入/MPPT 数据（以授权数据点为准） |
| 告警信息 | 告警等级、告警名称、发生时间、恢复时间、处理状态 |
| 统计分析 | 日、月、年发电量趋势和数据缺失标记 |

### 3.2 需注意的数据边界

- 逆变器可提供发电相关数据，但“校园总用电量、上网电量、自发自用率”等数据通常需要现场已接入智能电表，并且该电表已纳入 FusionSolar 电站。
- 实时性、历史数据粒度、可访问设备参数及告警字段由 FusionSolar 账号权限、地区站点和华为实际开通的 API 范围决定。
- 本期仅做数据读取和展示，不开通远程启停、功率限发、储能控制或其他写入型调度功能。

## 4. 前置条件及责任人

| 项目 | 完成标准 | 建议责任人 |
|---|---|---|
| 设备联网 | 逆变器已通过通信设备稳定接入 FusionSolar，并能在平台显示数据 | 设备安装商 / 运维人员 |
| 电站归属 | 电站已登记在学校或授权运维方的 FusionSolar 账号下 | 校方项目负责人 |
| 账号权限 | 明确电站业主、运维商、设备安装商及平台实施方的账号角色 | 校方项目负责人 |
| OpenAPI 资格 | 已由华为或授权服务商开通 FusionSolar OpenAPI / Northbound API 权限 | 华为对接人 / 授权服务商 |
| 应用凭据 | 已获取测试和生产环境的应用标识、密钥、授权方式和 API 文档 | 华为对接人 / 平台实施方 |
| 服务器能力 | 可发起 HTTPS 请求，具备密钥管理、数据库和定时任务能力 | 平台实施方 |
| 数据口径 | 已确认需要展示的发电量、功率、告警和统计口径 | 校方项目负责人 / 平台实施方 |

## 5. 账号与授权实施流程

### 步骤 1：确认电站已纳入 FusionSolar

1. 由设备安装商或学校账号管理员登录 FusionSolar，确认 `SUN2000-5KTL-L1` 已经加入目标电站。
2. 核对逆变器是否在线，并确认当日发电量、累计发电量和设备状态在 FusionSolar 中可见。
3. 记录电站名称、站点标识、设备名称和设备标识。设备序列号只在后端保存，Web 端应按需要脱敏。
4. 若电站当前归属于安装商个人或公司账号，应完成学校账号授权或账号交接后再对接。

### 步骤 2：申请 FusionSolar OpenAPI 账户

1. 向华为 FusionSolar 对接人或授权服务商说明用途：学校智慧农场平台读取自有电站的发电数据、设备状态和告警信息。
2. 明确申请的环境和范围：生产环境、目标电站、只读权限、数据查询 API。
3. 根据华为当前的开放平台流程创建或登记第三方应用，完成企业/账号/电站授权。
4. 获取并登记以下信息：应用 ID、应用密钥或证书要求、授权模式、令牌有效期、刷新方式、接口基础地址、限流规则、回调要求和技术支持联系人。
5. 使用单独的“校园智慧农场平台”应用账户，不与安装商个人账号共用凭据。

FusionSolar 官方资料说明，第三方应用可采用 OAuth Connect 模式：先取得访问令牌，再使用令牌调用电站列表等开放接口。具体后台菜单、授权页面及字段会因地区、账号类型和华为平台版本不同而变化，应以华为实际开通的文档为准。

### 步骤 3：安全保存凭据

1. 应用密钥、证书和刷新令牌只保存在校园平台后端的密钥管理服务或受权限保护的环境变量中。
2. 开发、测试、生产环境使用不同的凭据和不同的权限范围。
3. 不将凭据写入 Git 仓库、Markdown、前端构建产物、浏览器本地存储或普通日志。
4. 建立密钥轮换记录；人员变动、设备交接或疑似泄露时立即在 FusionSolar 侧撤销并重新签发凭据。

## 6. 后端对接实施步骤

### 步骤 1：实现认证模块

1. 建立 `fusionsolar_client` 后端模块，统一管理 OAuth 令牌申请、缓存、刷新、失效处理和接口重试。
2. 根据华为开通资料调用认证接口，使用应用凭据换取访问令牌。
3. 令牌仅在后端内存或安全缓存中使用；令牌过期前按官方要求刷新。
4. 认证失败时记录错误码和关联请求标识，不记录密钥、令牌或完整授权报文。

### 步骤 2：完成最小可用数据链路

按以下顺序调用已授权的 API：

1. 查询电站列表，定位学校目标电站并保存外部电站 ID。
2. 查询电站 KPI 或统计数据，获取当前功率、当日发电量和累计发电量。
3. 查询设备列表，定位 `SUN2000-5KTL-L1` 并保存外部设备 ID。
4. 查询逆变器运行状态及已授权的运行参数。
5. 查询活动告警和告警历史，建立告警同步机制。
6. 将每类数据的“源数据时间”和“平台入库时间”分开保存，便于识别延迟或断档。

不得在未获得明确授权和现场安全方案前调用任何设备控制、调度、限发或写入型接口。

### 步骤 3：建立数据模型和同步任务

| 数据表/实体 | 核心字段 |
|---|---|
| 光伏电站 | 外部电站 ID、名称、装机容量、状态、时区 |
| 光伏设备 | 外部设备 ID、型号、设备名称、运行状态、最后通信时间 |
| 发电指标 | 电站/设备 ID、指标类型、指标值、单位、源数据时间、入库时间 |
| 告警 | 外部告警 ID、设备 ID、等级、名称、发生时间、恢复时间、状态 |
| 接口调用日志 | 接口名称、请求时间、响应状态、错误码、关联标识 |

建议同步策略如下，最终以华为的接口限流和数据刷新周期为准：

| 数据 | 建议频率 | 说明 |
|---|---|---|
| 当前功率、设备状态 | 每 5 分钟 | 用于 Web 总览；遇到限流时按厂商要求降频 |
| 当日发电量 | 每 15 分钟 | 用于日累计展示和趋势图 |
| 设备清单 | 每日 1 次 | 设备新增、替换时可手动触发同步 |
| 活动告警 | 每 5 至 15 分钟 | 告警恢复状态需要一并同步 |
| 日/月统计 | 每日离峰时段补采 | 对前一日数据进行核对和补全 |

### 步骤 4：实现 Web 端

1. 在农场首页或能源页面展示当前光伏功率、当日发电量、累计发电量和数据更新时间。
2. 提供日、月、年发电量趋势图，明确标识数据缺失或延迟。
3. 提供设备状态页，展示逆变器在线状态、运行状态和最近通信时间。
4. 提供告警列表，展示告警等级、发生时间、恢复状态和关联设备。
5. 数据超过约定时间未更新时，显示“数据延迟/待核实”，不得继续显示为实时数据。
6. 本期 Web 页面不提供设备控制按钮。

## 7. 测试与验收

| 验收项 | 通过标准 |
|---|---|
| OpenAPI 授权 | 后端可成功取得令牌，且令牌不暴露给浏览器 |
| 电站识别 | 平台可准确定位学校目标电站，不混入其他电站 |
| 发电数据 | 当前功率、当日发电量、累计发电量与 FusionSolar 页面核对一致或在约定误差范围内 |
| 设备信息 | 平台正确识别 `SUN2000-5KTL-L1` 及其运行状态 |
| 告警同步 | 测试或历史告警可在约定同步周期内显示，恢复状态可更新 |
| 数据补采 | 出现短时 API 或网络异常后，任务可重试并补齐可获取的历史数据 |
| 数据隔离 | API 密钥、令牌和完整设备序列号不会出现在前端、日志或代码仓库 |
| 只读边界 | 平台没有调用设备控制或调度接口 |

## 8. 运维与安全要求

- 平台只申请满足业务需要的最小只读权限。
- 令牌、密钥和证书按华为要求轮换，并由专人保管。
- 接口调用发生鉴权失败、权限变更、连续超限或数据中断时，应向平台管理员告警。
- 发电量作为业务展示数据时，应注明数据来源为 FusionSolar；如涉及结算，应以电表及供电部门确认的数据为准。
- 逆变器离线、云端延迟或智能电表未接入时，平台应明确提示数据范围和有效性，避免错误推导自发自用率或用电量。

## 9. 待确认项

1. 目标电站是否已在 FusionSolar 上完成建站并由学校账号管理；
2. 学校是否已有华为 FusionSolar OpenAPI / Northbound API 申请渠道和对接人；
3. 是否只需要发电数据，还是还需要设备状态、告警、智能电表数据；
4. 现场是否已安装并接入华为兼容智能电表；
5. 华为为该账号开通的认证模式、接口版本、字段范围和限流规则；
6. 需要展示的数据刷新频率、历史数据保存期限和数据口径；
7. 是否存在其他电站或设备需要纳入同一平台，及其数据隔离要求。

## 10. 官方资料

- [Calling Open APIs by Third-Party Apps - Huawei Technical Support](https://support.huawei.com/enterprise/en/doc/EDOC1100465337/fa693885/calling-open-apis-by-third-party-apps)
- [FusionSolar Modbus TCP 配置说明（仅作本地接入参考，本方案不采用）](https://support.huawei.com/enterprise/en/doc/EDOC1100518312/2ca63b29/modbus-tcp)

> 注：FusionSolar 的应用注册入口、接口基础地址、认证参数、字段名称和限流策略受地区、账号类型及华为平台版本影响。实施时必须以华为对接人开通的最新 OpenAPI 文档和测试环境结果为准。
